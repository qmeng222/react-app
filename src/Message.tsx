// Create a function component (child) following Pascal Casing:
function Message() {
  const name = "Qingying";

  // JSX: JavaScript XML (Extensible Markup Language)
  if (name) return <h1>Hello, {name}!</h1>;
  return <h1>Hello, world!</h1>;
}

export default Message;
